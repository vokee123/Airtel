{
  "name": "employeed-loan-platform",
  "version": "1.0.0",
  "description": "Employed loan application platform with Airtel Money and Orange Money verification",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npx -y serve -l 3000",
    "dev:backend": "cd backend && npm start",
    "start": "npm run dev",
    "install:all": "npm install && cd backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
